apiVersion: v1
kind: Service
metadata:
  name: shopsmart-api
  namespace: shopsmart
  labels:
    app.kubernetes.io/name: shopsmart-api
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: shopsmart-api
  ports:
    - name: http
      port: 80
      targetPort: http
